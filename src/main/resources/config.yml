
#Placeholders:
#%minplayer%
#%waitingplayers%
#%arenaname%
#%neededplayers%
#%aliveplayers%
scoreboards:
  titles: "Pillars Of Fortune"
  lobby:
    - ""
    - "<green>Players: <white>%pillars_arenaname%"
    - "<green>Arenas: <white>5"
    - "<aqua>Kills: <white>0"
    - ""
  waiting:
    - ""
    - "<green>Arena: <white>%pillars_arenaname%"
    - "<green>Needed: <white>%pillars_waitingplayers%/%pillars_minplayers%"
    - "<aqua>State: <white>Waiting..."
    - ""
  in-game:
    - ""
    - "<green>Arena: <white>${arena.name}"
    - "<aqua>Alive: <white>$"
    - "<yellow>Kills: <white>0"
    - ""

spawn:
  world: "world"
  x: 1
  y: 1
  z: 1
  yaw: 1
  pitch: 1

lobby:
  # Set to false if another plugin fully manages the lobby world (a dedicated hub plugin,
  # for example): GourPillars will then leave the lobby alone entirely and only take care
  # of arenas/matches. Each check below only runs while this is true.
  enabled: true
  checks:
    # Teleport players to the configured spawn on join.
    teleport-to-spawn-on-join: true
    # Reset health/hunger/fire/potion effects on join.
    reset-state-on-join: true
    # Give the lobby-items (see lobby-items below) on join, and handle clicking them.
    lobby-items: true
    # Show the lobby scoreboard.
    scoreboard: true
    # Keep hunger at full while in the lobby world.
    unlimited-food: true
    # Block building/breaking/signs/interactions in the lobby world (build sessions are exempt).
    world-protection: true
    # Cancel all damage (including PvP) in the lobby world.
    damage-protection: true
    # Block dropping items/inventory clicks/hand swapping in the lobby world.
    item-protection: true
    # Teleport a player back to the spawn if they fall into the void while not in an active
    # match (no arena, or an arena that's waiting/starting/stopped).
    void-teleport-to-spawn: true

party:
  # A party's max size is normally the max player count of the biggest currently
  # loaded arena (in any state, including in-game ones), so a party can never be
  # too big to fit in any arena. This value is only used as a fallback when no
  # arena is loaded at all.
  fallback-max-size: 8

game:
  # Match length, in seconds.
  match-duration-seconds: 300
  # Pre-match countdown, in seconds.
  countdown-seconds: 10
  # How often (in seconds) each alive player receives a random item.
  random-item-interval-seconds: 3.5
  # Materials that will never be handed out by the random item task, e.g. because they're
  # creative-only/dev blocks, unobtainable in survival, or otherwise unfit as a random drop.
  # Uses Bukkit Material names: https://jd.papermc.io/paper/1.21/org/bukkit/Material.html
  excluded-random-items:
    - COMMAND_BLOCK
    - CHAIN_COMMAND_BLOCK
    - REPEATING_COMMAND_BLOCK
    - COMMAND_BLOCK_MINECART
    - BARRIER
    - STRUCTURE_BLOCK
    - STRUCTURE_VOID
    - JIGSAW
    - DEBUG_STICK
    - KNOWLEDGE_BOOK
    - LIGHT
    - END_PORTAL_FRAME
    - END_PORTAL
    - NETHER_PORTAL
    - BEDROCK
    - SPAWNER
    - TRIAL_SPAWNER
    - VAULT
    - REINFORCED_DEEPSLATE
    - ENDER_DRAGON_SPAWN_EGG
    - WITHER_SPAWN_EGG
    - SNOWBALL
    - SHIELD
  # Velocity multiplier applied to the victim when the "knockback" event is active.
  knockback-multiplier: 2.0
  # How often (in seconds) the lava rises when the "lava" event is active.
  lava-rise-interval-seconds: 4
  border:
    # Border size (in blocks) at which the shrink stops.
    final-size: 10.0
    # How often (in seconds) the border shrinks when the "border" event is active.
    shrink-interval-seconds: 7
    # Damage dealt per second to players outside the border.
    damage-amount: 3.0
  # Votes only make an event more likely, they never guarantee it: after the vote, one option
  # (including "no event") is picked at random, weighted by how many votes it received.
  events:
    lava:
      # If disabled, this event can't be voted for or picked, even with votes.
      enabled: true
    knockback:
      enabled: true
    border:
      enabled: true
    # Weight every option (including "no event") starts with before votes are counted, so an
    # option can still be picked with zero votes. Set to 0 to require at least one vote.
    base-weight: 1.0
    # Extra weight added per vote received.
    vote-weight: 1.0
  # Slot-machine style animation played once the vote closes, right before the match starts.
  event-selection-animation:
    enabled: true
    # How many names scroll by before landing on the picked event.
    scroll-count: 16
    # Delay (in ticks) between scrolled names at the start/end of the animation; it gradually
    # slows down from the start value to the end value for a "landing" effect.
    scroll-interval-start-ticks: 2
    scroll-interval-end-ticks: 7
    # Sound played for each scrolled name. See https://jd.papermc.io/paper/1.21/org/bukkit/Sound.html
    scroll-sound: BLOCK_NOTE_BLOCK_PLING
    scroll-sound-volume: 0.6
    scroll-sound-pitch: 1.4
    # Sound played once when the picked event is revealed.
    reveal-sound: BLOCK_NOTE_BLOCK_PLING
    reveal-sound-volume: 1.0
    reveal-sound-pitch: 2.0
    # How long (in ticks) the reveal stays on screen before the match actually starts.
    reveal-hold-ticks: 40

# Items given to players standing in the lobby (hotbar slots 0-8).
# Add, remove or reorder entries freely; each key is just an internal id.
# "command" is optional: leave it empty ("") for an item that does nothing when clicked.
lobby-items:
  mode-selector:
    slot: 4
    material: COMPASS
    name: "<gold><bold>Mode Selector"
    lore:
      - "<gray>Click to choose a game mode"
    command: ""
  cosmetics:
    slot: 6
    material: EMERALD
    name: "<gold><bold>Cosmetics"
    lore:
      - "<gray>Customize your appearance"
    command: ""
  casual-match:
    slot: 2
    material: NETHER_STAR
    name: "<gold><bold>Casual Match"
    lore:
      - "<gray>Join the best available arena"
    command: "joinrandom"

# Slot and material for the two items given to players in an arena's waiting
# room. Their name/lore come from language.yml (items.waiting.*) since what
# they do (open the vote menu / leave the arena) is fixed.
waiting-items:
  vote:
    slot: 0
    material: PAPER
  leave:
    slot: 8
    material: RED_DYE

# GUI opened by the "vote" waiting item, to vote for an event and day/night.
# Item names are reused by the event-selection-animation below.
gui:
  vote:
    title: "<light_purple>Vote"
    # Must be a multiple of 9, between 9 and 54.
    size: 27
    filler:
      material: GRAY_STAINED_GLASS_PANE
      name: "<gray>────</gray>"
    # Each key is a fixed id, don't rename them. Slot/material/name/lore are free
    # to change; removing an entry hides it (lava/knockback/border-event also
    # auto-hide when their event is disabled under game.events).
    # "material" accepts any Bukkit material, or PLAYER_HEAD with a "head-texture"
    # (base64 skin value, e.g. from minecraft-heads.com) for a custom head.
    items:
      no-event:
        slot: 10
        material: PLAYER_HEAD
        head-texture: "eyJ0ZXh0dXJlcyI6eyJTS0lOIjp7InVybCI6Imh0dHA6Ly90ZXh0dXJlcy5taW5lY3JhZnQubmV0L3RleHR1cmUvNGEyZmUwMWExZjdkNzZmM2NkNmRkYjUzZDUzMjVhMzk4YWQ3NDhkNzE4YWU3MjBhNmJjMjMzODI4NjdkNjUzMSJ9fX0="
        name: "<red>No event"
        lore:
          - "<gray>Vote for a classic game!"
      lava-event:
        slot: 11
        material: PLAYER_HEAD
        head-texture: "eyJ0ZXh0dXJlcyI6eyJTS0lOIjp7InVybCI6Imh0dHA6Ly90ZXh0dXJlcy5taW5lY3JhZnQubmV0L3RleHR1cmUvYjY5YTMyY2ZmZjAzMTU1YjlmODEwOTg1OGQ4MzAzYjA2ZmU3MGQwYjUzNWJhNjRiNTFkMDMwMmZmMzM5ZTBjYiJ9fX0="
        name: "<yellow>Lava event"
        lore:
          - "<gray>A lava floor will"
          - "<gray>appear under your feet!"
      knockback-event:
        slot: 12
        material: PLAYER_HEAD
        head-texture: "eyJ0ZXh0dXJlcyI6eyJTS0lOIjp7InVybCI6Imh0dHA6Ly90ZXh0dXJlcy5taW5lY3JhZnQubmV0L3RleHR1cmUvNjA2MWY5OGFhZmYxZTQwNmUwNmY2ZjEzZmZlMDYwMDU4NzNmM2QxZWFkZGIxYjU5ZTE5ZGRhMGVkOWZmYjI3MCJ9fX0="
        name: "<green>Knockback Event"
        lore:
          - "<gray>Players will get a"
          - "<gray>boosted knockback!"
      border-event:
        slot: 13
        material: PLAYER_HEAD
        head-texture: "eyJ0ZXh0dXJlcyI6eyJTS0lOIjp7InVybCI6Imh0dHA6Ly90ZXh0dXJlcy5taW5lY3JhZnQubmV0L3RleHR1cmUvZjk1MGNiNjVlNDFiMWQ2NjhjZjg4MmViODk4NWUwM2UxNzY2MTc5NTU2NjM2NDA3MTRmYzA4ZmJlZTFmMWE0NiJ9fX0="
        name: "<green>Border Event"
        lore:
          - "<gray>the arena border"
          - "<gray>will shrink!"
      day-vote:
        slot: 15
        material: PLAYER_HEAD
        head-texture: "eyJ0ZXh0dXJlcyI6eyJTS0lOIjp7InVybCI6Imh0dHA6Ly90ZXh0dXJlcy5taW5lY3JhZnQubmV0L3RleHR1cmUvYTUxZTUyYjFkZTk3YjUzNDM1OGI1M2NkMmM5YzQ1NTI2MDg2ZDJhNmYwZTBhZTY1ZTRiYTJmZjVjNjI5MGVjIn19fQ=="
        name: "<aqua>Day time"
        lore:
          - "<gray>A bright game!"
      night-vote:
        slot: 16
        material: PLAYER_HEAD
        head-texture: "eyJ0ZXh0dXJlcyI6eyJTS0lOIjp7InVybCI6Imh0dHA6Ly90ZXh0dXJlcy5taW5lY3JhZnQubmV0L3RleHR1cmUvYzZiMzIxNDRlNzAzMjcyNjE0MTdhYTEwN2U0Y2YyYjMyNTMxNWEzMmYxZDgxYzIxZDE5ZjBmNzhjNjEwMzhhOSJ9fX0="
        name: "<yellow>Night event"
        lore:
          - "<gray>A nighttime game!"
